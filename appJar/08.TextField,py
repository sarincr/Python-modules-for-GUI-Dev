 
from appJar import gui


app = gui()

# Adding text area
app.addTextArea("Box")

# Setting the default value
app.setTextArea("Box",
                "Hello World",
                end = True, callFunction = False)

app.go()
